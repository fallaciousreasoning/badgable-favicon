{
  "name": "favicon-badge",
  "version": "1.0.1",
  "description": "A web component for displaying a badgable favicon",
  "main": "dist/FavIconBadge.js",
  "types": "dist/FavIconBadge.d.ts",
  "scripts": {
    "build": "tsc && npm run minify",
    "prepublish": "npm run build",
    "minify": "terser dist/FavIconBadge.js --output dist/FavIconBadge.min.js --source-map 'content=dist/FavIconBadge.js.map'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fallaciousreasoning/badgable-favicon"
  },
  "keywords": [
    "favicon",
    "badge",
    "webcomponent"
  ],
  "author": "fallaciousreasoning",
  "license": "MIT",
  "devDependencies": {
    "terser": "^4.1.3",
    "typescript": "^3.5.3"
  }
}
