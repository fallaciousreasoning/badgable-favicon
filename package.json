{
  "name": "favicon-badge",
  "version": "1.0.1",
  "description": "A web component for displaying a badgable favicon",
  "main": "dist/BadgeableFavIcon.js",
  "types": "dist/BadgeableFavIcon.d.ts",
  "scripts": {
    "build": "tsc && npm run minify",
    "prepublish": "npm run build",
    "minify": "terser dist/BadgeableFavIcon.js --output dist/BadgeableFavIcon.min.js --source-map 'content=dist/BadgeableFavIcon.js.map'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fallaciousreasoning/badgable-favicon"
  },
  "keywords": [
    "favicon",
    "badge",
    "webcomponent"
  ],
  "author": "fallaciousreasoning",
  "license": "MIT",
  "devDependencies": {
    "terser": "^4.1.3",
    "typescript": "^3.5.3"
  }
}
